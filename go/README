Running unit tests:

  go test -v

Running coverage test:

  go test -coverprofile=coverage.out

To present the coverage test output on web browser

  go tool cover -html=coverage.out

Running all benchmark test (replace . with a given test for running it individually)
  go test -bench=.
